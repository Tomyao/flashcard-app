{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD -- . ':(exclude)server'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
