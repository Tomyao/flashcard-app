{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)server'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
